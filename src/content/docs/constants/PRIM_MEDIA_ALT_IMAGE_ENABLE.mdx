---
title: PRIM_MEDIA_ALT_IMAGE_ENABLE
slug: constants/PRIM_MEDIA_ALT_IMAGE_ENABLE
editorial: stub
category: constants
---
import LSLConstant from '/src/templates/LSLConstant.astro'

<LSLConstant
	name="PRIM_MEDIA_ALT_IMAGE_ENABLE"
	type="integer"
	value="0"
>
	<Fragment slot="description">
		Boolean. Gets/Sets the default image state (the image that the user sees before a piece of media is active) for the chosen face. The default image is specified by Second Life's server for that media type.
	</Fragment>
</LSLConstant>

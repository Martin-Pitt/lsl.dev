---
title: HTTP_ACCEPT
slug: constants/HTTP_ACCEPT
editorial: stub
category: constants
---
import LSLConstant from '/src/templates/LSLConstant.astro'

<LSLConstant
	name="HTTP_ACCEPT"
	type="integer"
	value="8"
>
	<Fragment slot="description">
		Provide a string value to be included in the HTTP
		            accepts header value. This replaces the default Second Life HTTP accepts header.
	</Fragment>
</LSLConstant>

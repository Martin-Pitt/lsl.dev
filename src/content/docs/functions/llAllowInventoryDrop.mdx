---
title: llAllowInventoryDrop
slug: functions/llAllowInventoryDrop
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llAllowInventoryDrop"
	arguments={[
	{
		"name": "Flag",
		"type": "integer",
		"description": "Boolean, If TRUE allows anyone to drop inventory on prim, FALSE revokes."
	}
]}
/>

If Flag == TRUE, users without object modify permissions can still drop inventory items into the object.

---
title: llClearLinkMedia
slug: functions/llClearLinkMedia
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llClearLinkMedia"
	returnType="integer"
	arguments={[
	{
		"name": "Link",
		"type": "integer"
	},
	{
		"name": "Face",
		"type": "integer"
	}
]}
/>

Clears (deletes) the media and all parameters from the given Face on the linked prim.

Returns an integer that is a STATUS_* flag, which details the success/failure of the operation.

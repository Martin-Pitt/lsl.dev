---
title: llCollisionFilter
slug: functions/llCollisionFilter
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llCollisionFilter"
	arguments={[
	{
		"name": "ObjectName",
		"type": "string"
	},
	{
		"name": "ObjectID",
		"type": "key"
	},
	{
		"name": "Accept",
		"type": "integer",
		"description": "If TRUE, only accept collisions with ObjectName name AND ObjectID (either is optional), otherwise with objects not ObjectName AND ObjectID."
	}
]}
/>

Specify an empty string or NULL_KEY for Accept, to not filter on the corresponding parameter.

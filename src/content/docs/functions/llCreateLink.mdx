---
title: llCreateLink
slug: functions/llCreateLink
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llCreateLink"
	arguments={[
	{
		"name": "TargetPrim",
		"type": "key",
		"description": "Object UUID that is in the same region."
	},
	{
		"name": "Parent",
		"type": "integer",
		"description": "If FALSE, then TargetPrim becomes the root. If TRUE, then the script's object becomes the root."
	}
]}
	sleep={0.1}
/>

Attempt to link the object the script is in, to target (requires permission PERMISSION_CHANGE_LINKS be set).

Requires permission PERMISSION_CHANGE_LINKS be set.

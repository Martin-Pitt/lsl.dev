---
title: llDetectedTouchPos
slug: functions/llDetectedTouchPos
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llDetectedTouchPos"
	returnType="vector"
	arguments={[
	{
		"name": "Index",
		"type": "integer",
		"description": "Index of detected information"
	}
]}
>
	<Fragment slot="description">
		Returns the position, in region coordinates, where the object was touched in a triggered touch event.
		
		Unless it is a HUD, in which case it returns the position relative to the attach point.
	</Fragment>
</LSLFunction>

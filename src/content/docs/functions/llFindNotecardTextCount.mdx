---
title: llFindNotecardTextCount
slug: functions/llFindNotecardTextCount
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llFindNotecardTextCount"
	returnType="key"
	arguments={[
	{
		"name": "NotecardName",
		"type": "string"
	},
	{
		"name": "Pattern",
		"type": "string",
		"description": "Regex pattern to find in the notecard text."
	},
	{
		"name": "Options",
		"type": "list",
		"description": "A list of options to control the search. Included for future expansion, should be []"
	}
]}
/>

Searches the text of a cached notecard for lines containing the given pattern and returns the 

            number of matches found through a dataserver event.

            

---
title: llFindNotecardTextSync
slug: functions/llFindNotecardTextSync
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llFindNotecardTextSync"
	returnType="list"
	arguments={[
	{
		"name": "NotecardName",
		"type": "string"
	},
	{
		"name": "Pattern",
		"type": "string",
		"description": "Regex pattern to find in the notecard text."
	},
	{
		"name": "StartMatch",
		"type": "integer",
		"description": "The number of matches to skip before returning values."
	},
	{
		"name": "Count",
		"type": "integer",
		"description": "The maximum number of matches to return. If 0 this function will return the first 64 matches found."
	},
	{
		"name": "Options",
		"type": "list",
		"description": "A list of options to control the search. Included for future expansion, should be []"
	}
]}
>
	<Fragment slot="description">
		Searches the text of a cached notecard for lines containing the given pattern. 
		
		            Returns a list of line numbers and column where a match is found. If the notecard is not in
		
		            the cache it returns a list containing a single entry of NAK. If no matches are found an
		
		            empty list is returned.
	</Fragment>
</LSLFunction>

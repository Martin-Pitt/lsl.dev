---
title: llGetAgentList
slug: functions/llGetAgentList
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llGetAgentList"
	returnType="list"
	arguments={[
	{
		"name": "Scope",
		"type": "integer",
		"description": "The scope (region, parcel, parcel same owner) to return agents for."
	},
	{
		"name": "Options",
		"type": "list",
		"description": "List of options to apply. Current unused."
	}
]}
/>

Requests a list of agents currently in the region, limited by the scope parameter.

Returns a list [key UUID-0, key UUID-1, ..., key UUID-n] or [string error_msg] - returns avatar keys for all agents in the region limited to the area(s) specified by scope

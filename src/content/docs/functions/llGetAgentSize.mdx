---
title: llGetAgentSize
slug: functions/llGetAgentSize
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llGetAgentSize"
	returnType="vector"
	arguments={[
	{
		"name": "AvatarID",
		"type": "key"
	}
]}
/>

If the avatar is in the same region, returns the size of the bounding box of the requested avatar by id, otherwise returns ZERO_VECTOR.

If the agent is in the same region as the object, returns the size of the avatar.

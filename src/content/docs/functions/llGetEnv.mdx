---
title: llGetEnv
slug: functions/llGetEnv
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llGetEnv"
	returnType="string"
	arguments={[
	{
		"name": "DataRequest",
		"type": "string",
		"description": "The type of data to request. Any other string will cause an empty string to be returned."
	}
]}
/>

Returns a string with the requested data about the region.

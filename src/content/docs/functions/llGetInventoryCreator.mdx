---
title: llGetInventoryCreator
slug: functions/llGetInventoryCreator
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llGetInventoryCreator"
	returnType="key"
	arguments={[
	{
		"name": "InventoryItem",
		"type": "string"
	}
]}
/>

Returns a key for the creator of the inventory item.

This function returns the UUID of the creator of item. If item is not found in inventory, the object says &quot;No item named 'name'&quot;.

---
title: llGetInventoryNumber
slug: functions/llGetInventoryNumber
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llGetInventoryNumber"
	returnType="integer"
	arguments={[
	{
		"name": "InventoryType",
		"type": "integer",
		"description": "Inventory item type"
	}
]}
/>

Returns the quantity of items of a given type (INVENTORY_* flag) in the prim's inventory.

Use the inventory constants INVENTORY_* to specify the type.

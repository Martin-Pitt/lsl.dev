---
title: llGetNumberOfNotecardLines
slug: functions/llGetNumberOfNotecardLines
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llGetNumberOfNotecardLines"
	returnType="key"
	arguments={[
	{
		"name": "NotecardName",
		"type": "string"
	}
]}
	sleep={0.1}
/>

Returns the number of lines contained within a notecard via the dataserver event.

The key returned by this function is a query ID for identifying the dataserver reply.

---
title: llGetPrimMediaParams
slug: functions/llGetPrimMediaParams
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llGetPrimMediaParams"
	returnType="list"
	arguments={[
	{
		"name": "Face",
		"type": "integer",
		"description": "face number"
	},
	{
		"name": "Parameters",
		"type": "list",
		"description": "One or more PRIM_MEDIA_* flags"
	}
]}
	sleep={1}
/>

Returns the media parameters for a particular face on an object, given the desired list of parameter names, in the order requested. Returns an empty list if no media exists on the face.

---
title: llGetRegionFlags
slug: functions/llGetRegionFlags
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llGetRegionFlags"
	returnType="integer"
/>

Returns the region flags (REGION_FLAG_*) for the region the object is in.

Returns a bit-field specifying the region flags (REGION_FLAG_*) for the region the object is in.

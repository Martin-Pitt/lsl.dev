---
title: llGetRegionTimeDilation
slug: functions/llGetRegionTimeDilation
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llGetRegionTimeDilation"
	returnType="float"
	arguments={[]}
>
	<Fragment slot="description">
		Returns the current time dilation as a float between 0.0 (full dilation) and 1.0 (no dilation).
		
		Returns the current time dilation as a float between 0.0 and 1.0.
	</Fragment>
</LSLFunction>

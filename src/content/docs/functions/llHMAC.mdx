---
title: llHMAC
slug: functions/llHMAC
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llHMAC"
	returnType="string"
	arguments={[
	{
		"name": "Key",
		"type": "string",
		"description": "The PEM-formatted key for the hash digest."
	},
	{
		"name": "Message",
		"type": "string",
		"description": "The message to be hashed."
	},
	{
		"name": "Algorithm",
		"type": "string",
		"description": "The digest algorithm: md5, sha1, sha224, sha256, sha384, sha512."
	}
]}
/>

Returns the base64-encoded hashed message authentication code (HMAC), of Message using PEM-formatted Key and digest Algorithm (md5, sha1, sha224, sha256, sha384, sha512).

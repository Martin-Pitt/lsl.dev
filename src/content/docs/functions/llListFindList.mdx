---
title: llListFindList
slug: functions/llListFindList
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llListFindList"
	returnType="integer"
	arguments={[
	{
		"name": "ListVariable",
		"type": "list"
	},
	{
		"name": "Find",
		"type": "list"
	}
]}
/>

Returns the index of the first instance of Find in ListVariable. Returns -1 if not found.

Returns the position of the first instance of the Find list in the ListVariable. Returns -1 if not found.

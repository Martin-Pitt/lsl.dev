---
title: llOverMyLand
slug: functions/llOverMyLand
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llOverMyLand"
	returnType="integer"
	arguments={[
	{
		"name": "ID",
		"type": "key"
	}
]}
/>

Returns TRUE if id ID over land owned by the script owner, otherwise FALSE.

Returns TRUE if key ID is over land owned by the object owner, FALSE otherwise.

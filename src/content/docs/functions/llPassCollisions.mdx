---
title: llPassCollisions
slug: functions/llPassCollisions
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llPassCollisions"
	arguments={[
	{
		"name": "Pass",
		"type": "integer",
		"description": "Boolean, if TRUE, collisions are passed from children on to parents."
	}
]}
/>

Configures how collision events are passed to scripts in the linkset.

If Pass == TRUE, collisions involving collision-handling scripted child prims are also passed on to the root prim. If Pass == FALSE (default behavior), such collisions will only trigger events in the affected child prim.

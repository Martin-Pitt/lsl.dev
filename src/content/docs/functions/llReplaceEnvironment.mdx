---
title: llReplaceEnvironment
slug: functions/llReplaceEnvironment
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llReplaceEnvironment"
	returnType="integer"
	arguments={[
	{
		"name": "position",
		"type": "vector",
		"description": "Location of parcel to change. Use &lt;-1, -1, -1&gt; for entire region."
	},
	{
		"name": "environment",
		"type": "string",
		"description": "Name of inventory item, or UUID of environment resource to apply.\n                         Use NULL_KEY or empty string to remove environment."
	},
	{
		"name": "track_no",
		"type": "integer",
		"description": "Elevation zone of where to apply environment. Use -1 for all."
	},
	{
		"name": "day_length",
		"type": "integer",
		"description": "Length of day cycle for this parcel or region. -1 to leave unchanged."
	},
	{
		"name": "day_offset",
		"type": "integer",
		"description": "Offset from GMT for the day cycle on this parcel or region. -1 to leave unchanged."
	}
]}
/>

Replaces the environment for a parcel or region.

---
title: llReturnObjectsByOwner
slug: functions/llReturnObjectsByOwner
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llReturnObjectsByOwner"
	returnType="integer"
	arguments={[
	{
		"name": "ID",
		"type": "key",
		"description": "Object owner's UUID."
	},
	{
		"name": "Scope",
		"type": "integer"
	}
]}
/>

Return objects based upon their owner and a scope of parcel, parcel owner, or region.

Requires the PERMISSION_RETURN_OBJECTS permission and that the script owner owns the parcel the returned objects are in, or is an estate manager or region owner.

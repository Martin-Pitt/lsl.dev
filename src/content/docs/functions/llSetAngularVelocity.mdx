---
title: llSetAngularVelocity
slug: functions/llSetAngularVelocity
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llSetAngularVelocity"
	arguments={[
	{
		"name": "AngVel",
		"type": "vector",
		"description": "The angular velocity to set the object to."
	},
	{
		"name": "Local",
		"type": "integer",
		"description": "If TRUE, the AngVel is treated as a local directional vector instead of a regional directional vector."
	}
]}
/>

Sets an object's angular velocity to AngVel, in local coordinates if Local == TRUE (if the script is physical).

Has no effect on non-physical objects.

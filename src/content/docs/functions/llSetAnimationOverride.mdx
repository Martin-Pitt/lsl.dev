---
title: llSetAnimationOverride
slug: functions/llSetAnimationOverride
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llSetAnimationOverride"
	arguments={[
	{
		"name": "AnimationState",
		"type": "string"
	},
	{
		"name": "AnimationName",
		"type": "string"
	}
]}
/>

Sets the animation (in object inventory) that will play for the given animation state.

To use this function the script must obtain the PERMISSION_OVERRIDE_ANIMATIONS permission.

---
title: llSetCameraAtOffset
slug: functions/llSetCameraAtOffset
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llSetCameraAtOffset"
	arguments={[
	{
		"name": "Offset",
		"type": "vector"
	}
]}
/>

Sets the camera used in this object, at offset, if an avatar sits on it.

Sets the offset that an avatar's camera will be moved to if the avatar sits on the object.

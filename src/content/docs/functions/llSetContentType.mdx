---
title: llSetContentType
slug: functions/llSetContentType
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llSetContentType"
	arguments={[
	{
		"name": "HTTPRequestID",
		"type": "key",
		"description": "A valid http_request() key"
	},
	{
		"name": "ContentType",
		"type": "integer",
		"description": "Media type to use with any following llHTTPResponse(HTTPRequestID, ...)"
	}
]}
/>

Set the media type of an LSL HTTP server response to ContentType.

HTTPRequestID must be a valid http_request ID. ContentType must be one of the CONTENT_TYPE_* constants.

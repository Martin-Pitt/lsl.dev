---
title: llSetInventoryPermMask
slug: functions/llSetInventoryPermMask
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llSetInventoryPermMask"
	arguments={[
	{
		"name": "InventoryItem",
		"type": "string",
		"description": "An item in the prim's inventory"
	},
	{
		"name": "PermissionFlag",
		"type": "integer",
		"description": "MASK_* flag"
	},
	{
		"name": "PermissionMask",
		"type": "integer",
		"description": "Permission bit-field (PERM_* flags)"
	}
]}
/>

Sets the given permission mask to the new value on the inventory item.

---
title: llSetLinkCamera
slug: functions/llSetLinkCamera
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llSetLinkCamera"
	arguments={[
	{
		"name": "LinkNumber",
		"type": "integer",
		"description": "Prim link number (0: unlinked, 1: root prim, &gt;1: child prims) or a LINK_* flag"
	},
	{
		"name": "EyeOffset",
		"type": "vector",
		"description": "Offset, relative to the object's centre and expressed in local coordinates, that the camera looks from."
	},
	{
		"name": "LookOffset",
		"type": "vector",
		"description": "Offset, relative to the object's centre and expressed in local coordinates, that the camera looks toward."
	}
]}
/>

Sets the camera eye offset, and the offset that camera is looking at, for avatars that sit on the linked prim.

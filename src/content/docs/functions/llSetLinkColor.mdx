---
title: llSetLinkColor
slug: functions/llSetLinkColor
editorial: stub
category: functions
---
import LSLFunction from '/src/content/templates/LSLFunction.astro'

<LSLFunction
	name="llSetLinkColor"
	arguments={[
	{
		"name": "LinkNumber",
		"type": "integer",
		"description": "Link number (0: unlinked, 1: root prim, &gt;1: child prims) or a LINK_* flag."
	},
	{
		"name": "Color",
		"type": "vector",
		"description": "Color in RGB &lt;R.R, G.G, B.B&gt;"
	},
	{
		"name": "Face",
		"type": "integer",
		"description": "Side number or ALL_SIDES."
	}
]}
/>

If a task exists in the link chain at LinkNumber, set the Face to color.

Sets the color of the linked child's side, specified by LinkNumber.

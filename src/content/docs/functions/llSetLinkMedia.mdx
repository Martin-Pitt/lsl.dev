---
title: llSetLinkMedia
slug: functions/llSetLinkMedia
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llSetLinkMedia"
	returnType="integer"
	arguments={[
	{
		"name": "Link",
		"type": "integer",
		"description": "Link number (0: unlinked, 1: root prim, &gt;1: child prims)."
	},
	{
		"name": "Face",
		"type": "integer",
		"description": "Face number."
	},
	{
		"name": "Parameters",
		"type": "list",
		"description": "A set of name/value pairs (in no particular order)"
	}
]}
/>

Set the media parameters for a particular face on linked prim, specified by Link. Returns an integer that is a STATUS_* flag which details the success/failure of the operation(s).

MediaParameters is a set of name/value pairs in no particular order. Parameters not specified are unchanged, or if new media is added then set to the default specified.

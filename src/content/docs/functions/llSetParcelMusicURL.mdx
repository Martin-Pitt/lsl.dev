---
title: llSetParcelMusicURL
slug: functions/llSetParcelMusicURL
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llSetParcelMusicURL"
	arguments={[
	{
		"name": "URL",
		"type": "string"
	}
]}
	sleep={2}
/>

Sets the streaming audio URL for the parcel the object is on.

The object must be owned by the owner of the parcel; if the parcel is group owned the object must be owned by that group.

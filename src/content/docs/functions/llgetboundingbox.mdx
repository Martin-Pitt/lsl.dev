---
title: llGetBoundingBox
slug: functions/llGetBoundingBox
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llGetBoundingBox"
	returnType="list"
	arguments={[
	{
		"name": "ID",
		"type": "key"
	}
]}
>
	<Fragment slot="description">
		Returns the bounding box around the object (including any linked prims) relative to its root prim, as a list in the format [ (vector) min_corner, (vector) max_corner ].
	</Fragment>
</LSLFunction>

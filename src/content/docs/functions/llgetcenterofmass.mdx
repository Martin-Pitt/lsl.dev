---
title: llGetCenterOfMass
slug: functions/llGetCenterOfMass
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llGetCenterOfMass"
	returnType="vector"
	arguments={[]}
>
	<Fragment slot="description">
		Returns the prim's centre of mass (unless called from the root prim, where it returns the object's centre of mass).
	</Fragment>
</LSLFunction>

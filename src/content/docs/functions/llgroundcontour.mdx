---
title: llGroundContour
slug: functions/llGroundContour
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llGroundContour"
	returnType="vector"
	arguments={[
	{
		"name": "Offset",
		"type": "vector"
	}
]}
>
	<Fragment slot="description">
		Returns the ground contour direction below the object position + Offset.
		
		Returns the ground contour at the object's position + Offset.
	</Fragment>
</LSLFunction>

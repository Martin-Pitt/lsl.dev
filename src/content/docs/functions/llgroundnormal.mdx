---
title: llGroundNormal
slug: functions/llGroundNormal
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llGroundNormal"
	returnType="vector"
	arguments={[
	{
		"name": "Offset",
		"type": "vector"
	}
]}
>
	<Fragment slot="description">
		Returns the ground normal below the object position + offset.
		
		Returns the ground contour at the object's position + Offset.
	</Fragment>
</LSLFunction>

---
title: llLinksetDataFindKeys
slug: functions/llLinksetDataFindKeys
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llLinksetDataFindKeys"
	returnType="list"
	arguments={[
	{
		"name": "search",
		"type": "string",
		"description": "A regex search string to match against keys in the datastore."
	},
	{
		"name": "start",
		"type": "integer",
		"description": "First entry to return. 0 for start of list."
	},
	{
		"name": "count",
		"type": "integer",
		"description": "Number of entries to return. Less than 1 for all keys."
	}
]}
>
	<Fragment slot="description">
		Returns a list of keys from the linkset's data store matching the search parameter.
	</Fragment>
</LSLFunction>

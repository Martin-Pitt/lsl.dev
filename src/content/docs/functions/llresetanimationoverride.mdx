---
title: llResetAnimationOverride
slug: functions/llResetAnimationOverride
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llResetAnimationOverride"
	returnType="void"
	arguments={[
	{
		"name": "AnimationState",
		"type": "string"
	}
]}
>
	<Fragment slot="description">
		Resets the animation of the specified animation state to the default value.
		
		If animation state equals &quot;ALL&quot;, then all animation states are reset.
	</Fragment>
</LSLFunction>

---
title: llSetRemoteScriptAccessPin
slug: functions/llSetRemoteScriptAccessPin
editorial: stub
category: functions
---
import LSLFunction from '/src/templates/LSLFunction.astro'

<LSLFunction
	name="llSetRemoteScriptAccessPin"
	returnType="void"
	arguments={[
	{
		"name": "PIN",
		"type": "integer"
	}
]}
	sleep={0.2}
>
	<Fragment slot="description">
		If PIN is set to a non-zero number, the task will accept remote script loads via llRemoteLoadScriptPin() if it passes in the correct PIN. Othersise, llRemoteLoadScriptPin() is ignored.
	</Fragment>
</LSLFunction>

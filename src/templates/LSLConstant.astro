---
import { FileTree } from "@astrojs/starlight/components";
const { name, type, value } = Astro.props;
---

<FileTree>
	<ul>
		<li>{name} {type} = {value}</li>
	</ul>
</FileTree>

{Astro.slots.has('description') && (<section>
<slot name="description"/>
</section>)}
